current_users = ['hasan', 'hussam', 'hikmat', 'jabbar', 'shamki']


new_users = ['muhammod', 'jawad', 'abbas', 'hasan', 'hussam']

for user in new_users:
    if user in current_users:
        print('this name is taken')
